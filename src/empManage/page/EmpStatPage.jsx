import React, { Fragment } from "react";
import { Container } from "../components";

// EmpStatPage. This page will show either personal details or more
// descriptive statistic of the currently logged employee for their own use.
function EmpStatPage({ auth }) {
	return (
		<Fragment>
			<Container />
		</Fragment>
	);
}

export default EmpStatPage;
