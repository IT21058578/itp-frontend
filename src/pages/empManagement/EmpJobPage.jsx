import React from "react";

// EmpJobPage. This page will list all the jobs an employee has. It will have
// two seperate tables for current jobs (they can decline these) and old jobs
function EmpJobPage({ auth }) {
	return (
		<div>
			EmpJobPage. This page will list all the jobs an employee has. It will have
			two seperate tables for current jobs (they can decline these) and old jobs
		</div>
	);
}

export default EmpJobPage;
